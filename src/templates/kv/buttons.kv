<PrimaryButton>:
    canvas.before:
        Color:
            rgba: self.background_color
        RoundedRectangle:
            radius: self.border_radius
            pos: self.pos
            size: self.size

    halign: 'center'
    font_size: '15sp'
    padding: '10sp', 0
